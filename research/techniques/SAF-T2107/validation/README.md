# Validation Evidence

This directory records isolated validation outputs for the frozen SAF-T2107 handoff. It contains no source corpus.

The strict validator was executed in a fresh synthetic-join repository containing only the bundle artifacts, the generic validator, and minimal placeholder neighbors and mitigations.
